---

prometheus_ipsec_exporter_version: v0.1.2.1

prometheus_ipsec_exporter_release_name: "ipsec_exporter-{{ prometheus_ipsec_exporter_version }}.linux-amd64"
prometheus_ipsec_exporter_url: "https://github.com/dennisstritzke/ipsec_exporter/releases/download/{{ prometheus_ipsec_exporter_version }}/{{ prometheus_ipsec_exporter_release_name }}.tar.gz"

prometheus_ipsec_exporter_config_flags:
  'web.listen-address': '0.0.0.0:9101'
  'collector.ipsec.conf': '/etc/ipsec.conf'
